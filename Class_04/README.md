# Topic

1. Use of Negative margin (when to use when not)

2. CSS specificity and ! important
specificity = Same attribute + multiple selector

    **** Priyority List ****
    0. !important
    1. Inline selector
    2. Id selector
    3. Class selector
    4. Tag selector

3. Pseudo-elements (::before, ::after)

4. Pseudo-classes (:hover, :focus, :not(), :last-child, :last-of-type, :nth-child())

5. Transform properties (translate, scale, screw, rotate etc.)

scale(1) = orginal  ; Zoom in, Zoom out

6. Typography Styles (Line-height, letter-specing, text-decoration, text-transform, font-size, color, overflow, etc.)

7. Units (px, em, rem, %, vw, vh, fr)

8. Colors (RGB, HEX, HSL, transparency)
